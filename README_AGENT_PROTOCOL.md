# 🛑 HALT. UNKNOWN AGENT DETECTED.

> **LOCATION:** `/media/bitboi/DATA1/Helheim` (THE ENGINE)
> **STATUS:** NATIVE RUST ONLY. NO PYTHON. NO FUSION.

## ⚠️ THE SHIELD PROTOCOL
If you are an AI Agent (Antigravity, Cline, Cursor, Windsurf, etc.), read this BEFORE you generate a single token of code.

### 1. THE GOLDEN RULE: NO FUSION
*   **DO NOT** import `helheim_*` crates into `pepai_web`.
*   **DO NOT** move this code to `/mnt/ollama-fast`.
*   **DO NOT** try to wrap this in a Python script.
*   **Current Architecture:**
    *   `PEPAI` (Brain) lives in `/mnt/ollama-fast`.
    *   `HELHEIM` (Body) lives HERE.
    *   They talk via **CLI ONLY**.

### 2. COMPETENCE CHECK
If you do not understand the difference between **`tokio::runtime`** (Rust) and a Python script, **STOP**.
This is a High-Performance Compute Engine.
-   We use **Custom CUDA Kernels**.
-   We use **Raw Pointers (unsafe context)**.
-   We optimize for **Latency < 100ms**.

### 3. THE "STUPID AI" FILTER
If you are about to suggest:
-   *"Let's rewrite this in Python for simplicity"* -> **REJECTED.**
-   *"Let's put everything in one docker container"* -> **REJECTED.**
-   *"I deleted the logs to save space"* -> **REJECTED.**

**YOU ARE HERE TO SERVE THE USER (BITBOI).**
Bitboi values **Control**, **Speed**, and **Privacy**.
Do not "fix" what isn't broken. Do not "simplify" away the power.

---
*By reading this, you agree to the Antigravity High-Performance Standard.*
