// Voorbeeld 1.1: Crash in voor_vertrek blokkeert de migratie volledig
handle Migratie { voor_vertrek => { gooi "CrashInVoorVertrek"; hervat(""); } } in { perform Swarm::migrate("127.0.0.1", 9000); }
